/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
char** partitionString(char* s, int* returnSize) {
    # Gemini did not return a valid solution for Partition String  in c.
}